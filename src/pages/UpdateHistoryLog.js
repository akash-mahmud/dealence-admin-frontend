import React from 'react'
import { useParams } from 'react-router-dom';

export default function UpdateHistoryLog() {
          const { id, contract, historyLogId } = useParams();
  return (
    <>
      
    </>
  )
}
